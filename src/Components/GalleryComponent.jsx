import React from 'react'
import ProjectDescription from './ProjectDescription'

function GalleryComponent (){
  return (
    <div>
      Gallery
    </div>
  
  )
}

// / *Create a component that would allow for twelve grid slots that will fit each image from a paticular Project. 
// When the project array iterates completetly. Create a new component that will create a line break. */ 



